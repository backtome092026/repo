import Link from "@/components/native-link";import {Cable,ChevronRight} from "lucide-react";import {isConfigured} from "@/lib/data";
export function SetupNotice(){if(isConfigured())return null;return <div className="mb-6 flex flex-wrap items-center gap-4 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-amber-950"><span className="grid size-10 place-items-center rounded-xl bg-amber-100"><Cable className="size-5"/></span><div className="min-w-0 flex-1"><p className="font-black">Ready for your school’s Supabase connection</p><p className="text-sm text-amber-800">No sample records are shown. Add your project credentials to begin using real data.</p></div><Link href="/staff/settings" className="flex items-center gap-1 text-sm font-black">Setup guide <ChevronRight className="size-4"/></Link></div>}
