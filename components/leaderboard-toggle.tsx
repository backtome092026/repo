"use client";import {useState} from "react";import {Switch} from "@/components/ui/switch";import {toast} from "sonner";
export function LeaderboardToggle({initial}:{initial:boolean}){const[on,setOn]=useState(initial);async function change(v:boolean){setOn(v);const r=await fetch("/api/student/profile",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({leaderboardOptIn:v})});if(r.ok)toast.success(v?"You joined the leaderboard":"Your name is now private");else{setOn(!v);toast.error("Preference could not be saved.")}}return <label className="flex items-center justify-between gap-4 rounded-2xl border bg-white p-5"><div><p className="font-black">Show me on leaderboards</p><p className="mt-1 text-sm text-slate-500">Only your display name, verified points, and activity count appear.</p></div><Switch checked={on} onCheckedChange={change} aria-label="Show me on leaderboards"/></label>}
