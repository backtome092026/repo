"use client";import {useState} from "react";import {Check,Loader2} from "lucide-react";import {Button} from "@/components/ui/button";import {Textarea} from "@/components/ui/textarea";import {Dialog,DialogContent,DialogHeader,DialogTitle,DialogTrigger} from "@/components/ui/dialog";import {toast} from "sonner";export function ReportReviewActions({id}:{id:string}){const[busy,setBusy]=useState(false),[open,setOpen]=useState(false);async function resolve(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget),r=await fetch(`/api/staff/reports/${id}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({status:"RESOLVED",note:String(f.get("note"))})});setBusy(false);if(r.ok){toast.success("Report resolved and recorded");setOpen(false);location.reload()}else toast.error("Report could not be updated.")}return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button size="sm"><Check/>Resolve</Button></DialogTrigger><DialogContent><DialogHeader><DialogTitle>Record the resolution</DialogTitle></DialogHeader><form onSubmit={resolve}><Textarea name="note" required rows={5} placeholder="What was reviewed and what action was taken?"/><Button disabled={busy} className="mt-4 w-full">{busy?<Loader2 className="animate-spin"/>:"Mark resolved"}</Button></form></DialogContent></Dialog>}
