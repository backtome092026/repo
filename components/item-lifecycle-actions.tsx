"use client";import {useState} from "react";import {CheckCircle2,Loader2,MoreHorizontal,Recycle,Trash2} from "lucide-react";import {Button} from "@/components/ui/button";import {DropdownMenu,DropdownMenuContent,DropdownMenuItem,DropdownMenuTrigger} from "@/components/ui/dropdown-menu";import {toast} from "sonner";import type {ItemStatus} from "@/types";
export function ItemLifecycleActions({id,status}:{id:string;status:ItemStatus}){const[busy,setBusy]=useState(false);async function update(next:ItemStatus){setBusy(true);const r=await fetch(`/api/staff/items/${id}/status`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({status:next})});setBusy(false);r.ok?(toast.success(`Item marked ${next.toLowerCase()}`),location.reload()):toast.error("Status could not be updated.")}if(["RETURNED","DONATED","DISPOSED"].includes(status))return null;return <DropdownMenu><DropdownMenuTrigger asChild><Button size="icon" variant="ghost" disabled={busy} aria-label="Item actions">{busy?<Loader2 className="animate-spin"/>:<MoreHorizontal/>}</Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem onClick={()=>update("RETURNED")}><CheckCircle2 className="text-emerald-600"/>Mark returned</DropdownMenuItem><DropdownMenuItem onClick={()=>update("DONATED")}><Recycle className="text-purple-600"/>Move to donation</DropdownMenuItem><DropdownMenuItem onClick={()=>update("DISPOSED")}><Trash2 className="text-rose-600"/>Mark disposed</DropdownMenuItem></DropdownMenuContent></DropdownMenu>}
