import Image from "next/image";import Link from "next/link";import {CalendarDays,MapPin} from "lucide-react";import {StatusBadge} from "@/components/status-badge";import type {Item} from "@/types";
export function ItemCard({item}:{item:Item}){return <Link href={`/item/${item.id}`} className="group overflow-hidden rounded-2xl border bg-white transition hover:-translate-y-1 hover:shadow-xl hover:shadow-slate-200/60"><div className="relative h-48 bg-[#edf1f6]"><Image src={item.photoUrl} alt={item.description} fill className="object-cover transition group-hover:scale-[1.03]"/></div><div className="p-5"><div className="mb-3 flex items-center justify-between gap-2"><span className="text-xs font-extrabold uppercase tracking-widest text-[#31598e]">{item.category}</span><StatusBadge status={item.status}/></div><h2 className="text-lg font-bold text-slate-900">{item.description}</h2><div className="mt-4 space-y-2 text-sm text-slate-500"><p className="flex items-center gap-2"><MapPin className="size-4"/>Found near {item.foundLocation}</p><p className="flex items-center gap-2"><CalendarDays className="size-4"/>{new Date(item.foundDate+"T12:00:00").toLocaleDateString("en-US",{month:"short",day:"numeric",year:"numeric"})}</p></div><p className="mt-4 border-t pt-3 text-xs font-bold tracking-wide text-slate-400">{item.itemNumber}</p></div></Link>}
