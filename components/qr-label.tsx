"use client";import {QRCodeSVG} from "qrcode.react";import {Printer} from "lucide-react";import {Button} from "@/components/ui/button";
export function QrLabel({id,itemNumber,description}:{id:string;itemNumber:string;description:string}){const url=typeof window==="undefined"?`https://back2me.example/item/${id}`:`${window.location.origin}/item/${id}`;return <div className="text-center"><div className="print-sheet mx-auto w-[360px] rounded-2xl border-2 border-[#172b4d] bg-white p-8"><p className="text-3xl font-black tracking-[-.05em] text-[#172b4d]">Back<span className="text-[#ea6b55]">2</span>Me</p><p className="mt-1 text-sm font-bold uppercase tracking-[.2em] text-slate-500">Lost & found, sorted.</p><div className="my-7 flex justify-center"><QRCodeSVG value={url} size={210} level="H" marginSize={2}/></div><p className="text-2xl font-black">{itemNumber}</p><p className="mt-2 text-sm text-slate-500">Scan to view this item</p><p className="mt-4 border-t pt-4 text-xs text-slate-400">{description}</p></div><Button className="no-print mt-6" onClick={()=>window.print()}><Printer/>Print QR label</Button></div>}
