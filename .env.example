NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Needed only by trusted setup/maintenance scripts; never expose in the browser.
SUPABASE_SERVICE_ROLE_KEY=
# Server-only Resend credentials for six-digit signup verification codes.
RESEND_API_KEY=
RESEND_FROM_EMAIL="Back2Me <onboarding@resend.dev>"
# Optional. Use a separate long random value in production; falls back to RESEND_API_KEY locally.
OTP_SIGNING_SECRET=
