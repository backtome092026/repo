import {NextResponse} from "next/server";import {createClient} from "@/lib/supabase/server";
type ClaimBody={itemId:string;studentName:string;studentEmail:string;contactInfo?:string;identifyingDetails:string;lostDate:string;lostLocation:string};
export async function POST(request:Request){try{const b=await request.json() as ClaimBody;if(!b.itemId||!b.studentName||!b.studentEmail||!b.identifyingDetails)return NextResponse.json({message:"Please complete all required fields."},{status:400});const s=await createClient();if(s){const{error}=await s.rpc("submit_claim",{p_item_id:b.itemId,p_student_name:b.studentName,p_student_email:b.studentEmail,p_contact_info:b.contactInfo||null,p_identifying_details:b.identifyingDetails,p_lost_date:b.lostDate,p_lost_location:b.lostLocation});if(error)throw error;}return NextResponse.json({ok:true});}catch{return NextResponse.json({message:"We couldn't submit your claim. Please try again."},{status:500});}}
