import {NextResponse} from "next/server";import {createClient} from "@/lib/supabase/server";export async function POST(r:Request){try{const b=await r.json() as{displayName:string;bio:string;wallEnabled:boolean;leaderboardOptIn:boolean},s=await createClient();if(!s)return NextResponse.json({}, {status:503});const{data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({}, {status:401});const{error}=await s.from("profiles").update({display_name:b.displayName,bio:b.bio,wall_enabled:b.wallEnabled,leaderboard_opt_in:b.leaderboardOptIn}).eq("id",user.id);if(error)throw error;return NextResponse.json({ok:true})}catch{return NextResponse.json({}, {status:400})}}
