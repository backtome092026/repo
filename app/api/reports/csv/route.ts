import {getDashboardData} from "@/lib/data";import {calculateImpact} from "@/lib/impact";export async function GET(){const{items,settings}=await getDashboardData(),m=calculateImpact(items,settings);const csv=`Metric,Value\nItems found,${m.total}\nItems returned,${m.returned}\nRecovery rate,${m.recoveryRate}%\nUnclaimed,${m.active}\nDonated,${m.donated}\nDisposed,${m.disposed}\nEstimated value recovered,$${m.valueRecovered}\nEstimated staff time saved,${m.staffHours} hours\n`;return new Response(csv,{headers:{"content-type":"text/csv","content-disposition":"attachment; filename=back2me-impact.csv"}})}
