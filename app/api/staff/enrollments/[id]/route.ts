import {NextResponse} from "next/server";import {createClient} from "@/lib/supabase/server";export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params,{decision}=await r.json() as{decision:"APPROVED"|"DENIED"},s=await createClient();if(!s)return NextResponse.json({}, {status:503});const{error}=await s.rpc("review_enrollment",{p_request_id:id,p_decision:decision,p_note:null});if(error)throw error;return NextResponse.json({ok:true})}catch{return NextResponse.json({}, {status:400})}}
