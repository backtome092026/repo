import {Award,Gift,HeartHandshake,Medal,Trophy} from "lucide-react";import {PageHero} from "@/components/page-hero";import {PublicHeader} from "@/components/public-header";import {Tabs,TabsContent,TabsList,TabsTrigger} from "@/components/ui/tabs";import {getLeaderboards} from "@/lib/data";import type {LeaderboardRow} from "@/types";
export default async function Leaderboard(){const boards=await getLeaderboards();return <><PublicHeader/><main className="mx-auto min-h-screen max-w-5xl px-5 py-8 lg:px-8"><PageHero eyebrow="Community champions" title="Small actions. Big school spirit." description="Celebrate students who return lost items, share what they can, and make life better for others."/><Tabs defaultValue="returns" className="mt-8"><TabsList className="grid h-auto w-full grid-cols-3 rounded-2xl bg-white p-2"><TabsTrigger className="rounded-xl py-3" value="returns">Item finders</TabsTrigger><TabsTrigger className="rounded-xl py-3" value="donations">Donors</TabsTrigger><TabsTrigger className="rounded-xl py-3" value="kindness">Kindness</TabsTrigger></TabsList><TabsContent value="returns"><Board rows={boards.returns} icon={Medal} unit="items returned"/></TabsContent><TabsContent value="donations"><Board rows={boards.donations} icon={Gift} unit="items given"/></TabsContent><TabsContent value="kindness"><Board rows={boards.kindness} icon={HeartHandshake} unit="good actions"/></TabsContent></Tabs><p className="mt-5 text-center text-sm text-slate-500">Students appear only when they choose to join the leaderboard. Schools may review all awards.</p></main></>}
function Board({rows,icon:Icon,unit}:{rows:LeaderboardRow[];icon:typeof Award;unit:string}){if(!rows.length)return <div className="mt-5 rounded-2xl border-2 border-dashed bg-white p-10 text-center"><Trophy className="mx-auto size-14 text-amber-300"/><h2 className="mt-4 text-2xl font-black">No rankings yet</h2><p className="mt-2 text-slate-600">The board will fill from verified school activity—not made-up scores.</p></div>;return <div className="mt-5 space-y-3">{rows.map((r,index)=><div key={r.studentId} className={`flex items-center gap-4 rounded-2xl border bg-white p-4 ${index<3?"pop-card":""}`}><span className={`grid size-12 place-items-center rounded-2xl text-xl font-black ${index===0?"bg-amber-100 text-amber-700":index===1?"bg-slate-200 text-slate-700":index===2?"bg-orange-100 text-orange-700":"bg-indigo-50 text-indigo-600"}`}>{index+1}</span><div className="min-w-0 flex-1"><p className="truncate text-lg font-black">{r.studentName}</p><p className="text-sm text-slate-500">{r.count} {unit}</p></div><div className="text-right"><Icon className="ml-auto size-5 text-[#ff6f61]"/><p className="mt-1 font-black">{r.score} pts</p></div></div>)}</div>}
