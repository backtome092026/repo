import Image from "next/image";import Link from "next/link";import {ArrowLeft,CalendarDays,MapPin,ShieldCheck} from "lucide-react";import {notFound} from "next/navigation";import {PublicHeader} from "@/components/public-header";import {StatusBadge} from "@/components/status-badge";import {Button} from "@/components/ui/button";import {getItem} from "@/lib/data";
export default async function ItemPage({params}:{params:Promise<{id:string}>}){const{id}=await params,item=await getItem(id);if(!item)notFound();const available=!["RETURNED","DONATED","DISPOSED"].includes(item.status);return <><PublicHeader/><main className="mx-auto min-h-screen max-w-6xl px-5 py-10 lg:px-8"><Link href="/search" className="inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft className="size-4"/>Back to search</Link><div className="mt-6 grid gap-10 lg:grid-cols-2"><div className="relative aspect-[4/3] overflow-hidden rounded-3xl border bg-[#edf1f6]"><Image src={item.photoUrl} alt={item.description} fill className="object-cover"/></div><div className="py-2"><div className="flex items-center gap-3"><span className="text-sm font-extrabold uppercase tracking-widest text-[#31598e]">{item.category}</span><StatusBadge status={item.status}/></div><h1 className="mt-4 text-4xl font-black tracking-[-.04em] text-[#172b4d]">{item.description}</h1><p className="mt-3 font-bold text-slate-400">{item.itemNumber}</p><div className="mt-8 grid gap-4 sm:grid-cols-2"><Info icon={CalendarDays} label="Date found" value={new Date(item.foundDate+"T12:00:00").toLocaleDateString("en-US",{month:"long",day:"numeric",year:"numeric"})}/><Info icon={MapPin} label="Found near" value={item.foundLocation}/></div><div className="mt-4 rounded-2xl border bg-white p-5"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Where it is now</p><p className="mt-1 text-lg font-bold">{item.publicLocation}</p><p className="mt-2 text-sm text-slate-500">Exact storage details are shared with staff only.</p></div>{available?<Button size="lg" asChild className="mt-6 h-13 w-full bg-[#ea6b55] text-base hover:bg-[#d95d49]"><Link href={`/claim/${item.id}`}>This might be mine</Link></Button>:<div className="mt-6 rounded-xl bg-slate-100 p-4 text-center font-semibold text-slate-600">This item is no longer available to claim.</div>}<p className="mt-5 flex items-start gap-2 text-sm leading-6 text-slate-500"><ShieldCheck className="mt-0.5 size-4 shrink-0 text-[#2f8b72]"/>To protect the owner, some identifying details are intentionally not shown.</p></div></div></main></>}
function Info({icon:Icon,label,value}:{icon:typeof CalendarDays;label:string;value:string}){return <div className="rounded-2xl border bg-white p-5"><Icon className="size-5 text-[#ea6b55]"/><p className="mt-4 text-xs font-bold uppercase tracking-wider text-slate-400">{label}</p><p className="mt-1 font-bold">{value}</p></div>}
