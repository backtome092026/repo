import Link from "@/components/native-link";import {Clock3,MailCheck,School,ShieldCheck} from "lucide-react";import {Brand} from "@/components/brand";import {Button} from "@/components/ui/button";export default async function Pending({searchParams}:{searchParams:Promise<{denied?:string}>}){const denied=(await searchParams).denied;return <main className="paper-grid grid min-h-screen place-items-center p-6"><div className="w-full max-w-xl rounded-[2rem] border bg-white p-8 text-center shadow-xl"><Brand/><span className={`mx-auto mt-8 grid size-20 place-items-center rounded-full ${denied?"bg-rose-100 text-rose-600":"bg-amber-100 text-amber-700"}`}>{denied?<ShieldCheck className="size-9"/>:<Clock3 className="size-9"/>}</span><h1 className="mt-5 text-3xl font-black text-[#192b67]">{denied?"Your request wasn’t approved":"Your school is checking your request"}</h1><p className="mt-3 leading-7 text-slate-600">{denied?"Contact your school office if you think this was a mistake or need to update your enrollment information.":"You’ve finished signup. A staff member will verify that you attend the school before your community access opens."}</p><div className="mt-7 grid gap-3 text-left sm:grid-cols-2"><Step icon={MailCheck} text="Confirm your email"/><Step icon={School} text="School verifies enrollment"/></div><Button asChild className="mt-7"><Link href="/staff/login">Check again</Link></Button></div></main>}
function Step({icon:Icon,text}:{icon:typeof School;text:string}){return <div className="flex items-center gap-3 rounded-xl bg-slate-50 p-4 text-sm font-black"><Icon className="size-5 text-indigo-600"/>{text}</div>}
