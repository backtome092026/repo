import Link from "next/link";import {ArrowLeft} from "lucide-react";import {notFound} from "next/navigation";import {QrLabel} from "@/components/qr-label";import {StaffShell} from "@/components/staff-shell";import {getItem} from "@/lib/data";
export default async function Label({params}:{params:Promise<{id:string}>}){const{id}=await params,item=await getItem(id);if(!item)notFound();return <StaffShell title="Printable QR label"><Link href="/staff/items" className="no-print mb-6 inline-flex items-center gap-2 text-sm font-bold text-slate-500"><ArrowLeft className="size-4"/>Back to items</Link><QrLabel id={item.id} itemNumber={item.itemNumber} description={item.description}/></StaffShell>}
