import Link from "next/link";import {PackageOpen,Plus,QrCode} from "lucide-react";import {ItemLifecycleActions} from "@/components/item-lifecycle-actions";import {StaffShell} from "@/components/staff-shell";import {StatusBadge} from "@/components/status-badge";import {Button} from "@/components/ui/button";import {getDashboardData} from "@/lib/data";
export default async function Items(){const{items}=await getDashboardData();return <StaffShell title="Lost & found" action={<Button asChild className="bg-[#ff6f61] hover:bg-[#e85e52]"><Link href="/staff/items/new"><Plus/>Add item</Link></Button>}><div className="overflow-hidden rounded-2xl border bg-white"><div className="flex flex-wrap items-center justify-between gap-4 border-b p-5"><div><h2 className="text-lg font-black">All item records</h2><p className="text-sm text-slate-500">Complete returns and keep every outcome accurate.</p></div><span className="rounded-full bg-indigo-50 px-3 py-1 text-sm font-black text-indigo-700">{items.length} items</span></div>{items.length?<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr>{["Item","Number","Found","Location","Status","Label","Actions"].map(h=><th className="px-5 py-3" key={h}>{h}</th>)}</tr></thead><tbody className="divide-y">{items.map(i=><tr key={i.id} className="hover:bg-slate-50/70"><td className="px-5 py-4 font-bold">{i.description}</td><td className="px-5 py-4 text-slate-500">{i.itemNumber}</td><td className="px-5 py-4">{i.foundDate}</td><td className="px-5 py-4">{i.foundLocation}</td><td className="px-5 py-4"><StatusBadge status={i.status}/></td><td className="px-5 py-4"><Button asChild size="sm" variant="ghost"><Link href={`/staff/items/${i.id}/label`}><QrCode/>QR</Link></Button></td><td className="px-5 py-4"><ItemLifecycleActions id={i.id} status={i.status}/></td></tr>)}</tbody></table></div>:<div className="grid min-h-80 place-items-center p-8 text-center"><div><PackageOpen className="mx-auto size-14 text-indigo-200"/><h2 className="mt-4 text-2xl font-black">Ready for the first found item</h2><p className="mt-2 text-slate-500">Photos, QR labels, claims, and history begin here.</p><Button asChild className="mt-5"><Link href="/staff/items/new"><Plus/>Log an item</Link></Button></div></div>}</div></StaffShell>}
