import {Gift,HeartHandshake,Medal,Trophy} from "lucide-react";import {StaffShell} from "@/components/staff-shell";import {getLeaderboards} from "@/lib/data";import type {LeaderboardRow} from "@/types";
export default async function StaffLeaderboard(){const b=await getLeaderboards();return <StaffShell title="Student impact leaderboards"><div className="grid gap-6 xl:grid-cols-3"><Board title="Top item returners" icon={Medal} rows={b.returns} unit="returns"/><Board title="Top student donors" icon={Gift} rows={b.donations} unit="donations"/><Board title="Kindness champions" icon={HeartHandshake} rows={b.kindness} unit="actions"/></div><div className="mt-6 rounded-2xl border border-indigo-200 bg-indigo-50 p-5 text-sm text-indigo-900"><strong>Privacy and fairness:</strong> only students who opt in appear by name. Points come from verified outcomes, and school staff retain moderation control.</div></StaffShell>}
function Board({title,icon:Icon,rows,unit}:{title:string;icon:typeof Trophy;rows:LeaderboardRow[];unit:string}){return <section className="rounded-2xl border bg-white p-5"><Icon className="size-7 text-[#ff6f61]"/><h2 className="mt-3 text-xl font-black">{title}</h2>{rows.length?<div className="mt-5 divide-y">{rows.map((r,i)=><div key={r.studentId} className="flex items-center gap-3 py-3"><span className="grid size-9 place-items-center rounded-xl bg-indigo-50 font-black text-indigo-700">{i+1}</span><div className="min-w-0 flex-1"><p className="truncate font-black">{r.studentName}</p><p className="text-xs text-slate-500">{r.count} {unit}</p></div><strong>{r.score}</strong></div>)}</div>:<p className="mt-5 rounded-xl bg-slate-50 p-5 text-center text-sm text-slate-500">No verified activity yet.</p>}</section>}
