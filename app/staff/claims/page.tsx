import {ClaimActions} from "@/components/claim-actions";import {StaffShell} from "@/components/staff-shell";import {getDashboardData} from "@/lib/data";
export default async function Claims(){const{claims,items}=await getDashboardData();return <StaffShell title="Claims"><div className="grid gap-5">{claims.length?claims.map(c=>{const item=items.find(i=>i.id===c.itemId);return <article key={c.id} className="rounded-2xl border bg-white p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><span className="rounded-full bg-amber-50 px-2.5 py-1 text-xs font-bold text-amber-800">{c.status}</span><h2 className="mt-3 text-xl font-black">{item?.description||"Item claim"}</h2><p className="mt-1 text-sm text-slate-500">{item?.itemNumber} · Submitted {new Date(c.createdAt).toLocaleDateString()}</p></div>{c.status==="PENDING"&&<ClaimActions id={c.id}/>}</div><div className="mt-5 grid gap-5 rounded-xl bg-slate-50 p-5 sm:grid-cols-3"><Info label="Student"><strong>{c.studentName}</strong><p className="text-sm">{c.studentEmail}</p></Info><Info label="Lost near"><strong>{c.lostLocation}</strong><p className="text-sm">{c.lostDate}</p></Info><Info label="Private identifying details"><p className="text-sm font-semibold">{c.identifyingDetails}</p></Info></div></article>}):<div className="rounded-2xl border-2 border-dashed bg-white p-10 text-center"><h2 className="text-2xl font-black">No claims yet</h2><p className="mt-2 text-slate-500">Student claims will appear here for private review.</p></div>}</div></StaffShell>}
function Info({label,children}:{label:string;children:React.ReactNode}){return <div><p className="mb-1 text-xs font-bold uppercase tracking-wider text-slate-400">{label}</p>{children}</div>}
