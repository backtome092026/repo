import Link from "@/components/native-link";import {ArrowLeft} from "lucide-react";import {notFound} from "next/navigation";import {ClaimForm} from "@/components/claim-form";import {PublicHeader} from "@/components/public-header";import {getItem} from "@/lib/data";
export default async function ClaimPage({params}:{params:Promise<{id:string}>}){const{id}=await params,item=await getItem(id);if(!item)notFound();return <><PublicHeader/><main className="mx-auto min-h-screen max-w-3xl px-5 py-10"><Link href={`/item/${item.id}`} className="inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft className="size-4"/>Back to item</Link><div className="mt-6 rounded-3xl border bg-white p-6 shadow-sm sm:p-10"><p className="text-sm font-bold uppercase tracking-wider text-[#ea6b55]">{item.itemNumber}</p><h1 className="mt-2 text-3xl font-black tracking-tight text-[#172b4d]">Tell staff why this is yours.</h1><p className="mb-8 mt-2 text-slate-600">For {item.description}. Your claim starts as pending while school staff verify it.</p><ClaimForm itemId={item.id} itemNumber={item.itemNumber}/></div></main></>}
